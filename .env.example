# ============================================
# BrainTestStudio - Umgebungsvariablen
# ============================================
# WICHTIG: Kopiere diese Datei zu .env und passe die Werte an!
# Die .env Datei wird NICHT in Git committed!
# ============================================

# --------------------------------------------
# ALLGEMEINE KONFIGURATION
# --------------------------------------------
# Domain für das Projekt (localhost für Entwicklung, später z.B. dashboard.intranet)
SITE_URL=http://localhost:3000
API_EXTERNAL_URL=http://localhost:8000

# E-Mail Adresse für Benachrichtigungen
ADMIN_EMAIL=deine-email@example.com

# --------------------------------------------
# DOCKER VOLUMES (Pfade relativ zum Projektordner)
# --------------------------------------------
DOCKER_DATA_PATH=./docker-data
POSTGRES_DATA_PATH=./docker-data/postgres
N8N_DATA_PATH=./docker-data/n8n
SUPABASE_STORAGE_PATH=./docker-data/supabase-storage
OLLAMA_DATA_PATH=./docker-data/ollama

# --------------------------------------------
# PORTS
# --------------------------------------------
FRONTEND_PORT=3000
N8N_PORT=5678
SUPABASE_STUDIO_PORT=3001
SUPABASE_API_PORT=8000
POSTGRES_PORT=5432
OLLAMA_PORT=11434

# --------------------------------------------
# POSTGRESQL / SUPABASE DATENBANK
# --------------------------------------------
# WICHTIG: Generiere sichere Passwörter für Produktion!
POSTGRES_PASSWORD=dein-sicheres-postgres-passwort
POSTGRES_USER=postgres
POSTGRES_DB=postgres

# --------------------------------------------
# SUPABASE KONFIGURATION
# --------------------------------------------
# JWT Secret (min. 32 Zeichen) - Generiere mit: openssl rand -base64 32
JWT_SECRET=dein-super-sicheres-jwt-secret-min-32-zeichen

# Supabase Keys - Generiere diese mit dem JWT_SECRET
# Anon Key: Für öffentliche API-Zugriffe (eingeschränkte Rechte)
ANON_KEY=dein-anon-key

# Service Role Key: Für Server-seitige Zugriffe (volle Rechte) - NIEMALS im Frontend verwenden!
SERVICE_ROLE_KEY=dein-service-role-key

# Dashboard Passwort für Supabase Studio
DASHBOARD_PASSWORD=dein-dashboard-passwort

# --------------------------------------------
# N8N KONFIGURATION
# --------------------------------------------
# Webhook URL für externe Aufrufe
N8N_WEBHOOK_URL=http://localhost:5678

# Verschlüsselungsschlüssel für Credentials - Generiere mit: openssl rand -hex 32
N8N_ENCRYPTION_KEY=dein-n8n-encryption-key

# Basic Auth für N8N (wird bei erster Registrierung überschrieben)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=dein-n8n-passwort

# --------------------------------------------
# OLLAMA KONFIGURATION
# --------------------------------------------
# API Key für Ollama - Generiere mit: openssl rand -hex 32
# Verwende diesen Key in N8N als Authorization Header
OLLAMA_API_KEY=sk-dein-ollama-api-key

# --------------------------------------------
# SMTP KONFIGURATION (Optional - für später)
# --------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SENDER_NAME=BrainTestStudio
